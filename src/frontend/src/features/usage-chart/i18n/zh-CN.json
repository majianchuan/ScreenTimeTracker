{
  "chart": {
    "titleWithAverage": "$t(shared:common.total)：{{total}} | $t(shared:common.average)：{{average}}",
    "titleWithoutAverage": "$t(shared:common.total)：{{total}}",
    "averageLabel": "$t(shared:common.average)\n{{value}}"
  }
}
