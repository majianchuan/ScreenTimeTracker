<UserControl x:Class="ScreenTimeTracker.Hosts.Desktop.NotifyIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ScreenTimeTracker.Hosts.Desktop"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             mc:Ignorable="d">


    <tb:TaskbarIcon
        x:Name="TrayIcon"
        IconSource="/Resources/Icon.ico"
        ToolTipText="ScreenTimeTracker"
        LeftClickCommand="{Binding OpenUICommand}"
        NoLeftClickDelay="True">

        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem
                    Header="打开程序目录"
                    Command="{Binding OpenAppDirectoryCommand}"/>
                <Separator/>
                <MenuItem
                    Header="在浏览器打开界面"
                    Command="{Binding OpenUIInBrowserCommand}"/>
                <MenuItem
                    Header="在窗口打开界面"
                    Command="{Binding OpenUIInWindowCommand}"/>
                <Separator/>
                <MenuItem
                    Header="退出"
                    Command="{Binding ExitApplicationCommand}"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>

    </tb:TaskbarIcon>
</UserControl>
